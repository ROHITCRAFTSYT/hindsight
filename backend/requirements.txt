# Core runtime — everything Hindsight needs for Cognee Cloud mode and demo mode.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27

# NOTE: Self-hosted / open-source mode (LLM_API_KEY set) additionally needs the
# embedded Cognee SDK. It is intentionally NOT pinned here because it is a large
# dependency and is unnecessary for Cognee Cloud and demo modes. Install it
# separately when running self-hosted, e.g.:
#     pip install "cognee[groq]"

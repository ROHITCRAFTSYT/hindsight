fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27

# Cognee memory layer. Installed for self-hosted / cloud modes.
# DEMO_MODE works without it, so it is optional at runtime but
# recommended for the real hackathon submission.
cognee>=0.1.15

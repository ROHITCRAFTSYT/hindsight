# Runtime deps for the Vercel serverless function. Kept minimal on purpose:
# this deployment runs in DEMO_MODE (see root vercel.json), so neither
# uvicorn (no server to bind) nor the cognee SDK (self-hosted mode only)
# is needed here.
fastapi>=0.141.1
pydantic>=2.13.5
python-dotenv>=1.2.3
httpx>=0.28.1
